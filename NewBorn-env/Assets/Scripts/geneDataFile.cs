using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class geneDataFile : MonoBehaviour {

	// Use this for initialization
	void Awake () {
		//System.IO.File.WriteAllText(System.Environment.CurrentDirectory + "/GeneData" + transform.name + ".txt", "GENE CODE");
	}
}
